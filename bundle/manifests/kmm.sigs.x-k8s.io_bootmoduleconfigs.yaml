apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  creationTimestamp: null
  labels:
    app.kubernetes.io/component: kmm
    app.kubernetes.io/name: kmm
    app.kubernetes.io/part-of: kmm
  name: bootmoduleconfigs.kmm.sigs.x-k8s.io
spec:
  group: kmm.sigs.x-k8s.io
  names:
    kind: BootModuleConfig
    listKind: BootModuleConfigList
    plural: bootmoduleconfigs
    shortNames:
    - bmc
    singular: bootmoduleconfig
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: BootModuleConfig describes how to load a module during kernel
          initialization
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              firmwareFilesPath:
                description: path of the firmware files in the kernel module container
                  image
                type: string
              inTreeModuleToRemove:
                description: the in-tree kernel module to remove prior to loading
                  the OOT kernel module
                type: string
              kernelModuleImage:
                description: kernel module container image that contains the kernel
                  module .ko file
                type: string
              kernelModuleName:
                description: the name of the kernel module to be loaded(the name of
                  the .ko file without the .ko)
                type: string
              machineConfigName:
                description: machine config that is targeted by the BMC
                type: string
              machineConfigPoolName:
                description: the machine config pool that is linked to the targeted
                  machine config
                type: string
              workerImage:
                description: KMM worker image. if missing, the current worker image
                  will be used
                type: string
            required:
            - kernelModuleImage
            - kernelModuleName
            - machineConfigName
            - machineConfigPoolName
            type: object
          status:
            properties:
              configStatus:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
